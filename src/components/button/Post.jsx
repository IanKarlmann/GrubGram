import { Button } from '@mui/material';

function Post() {
    return (
      <Button variant="contained" color="primary" onClick={() => alert('Button Clicked')}>
        Post
      </Button>
    );
}
  
export default Post;


